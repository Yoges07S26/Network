int totalHammingDistance(int* nums, int numsSize) {
    int total_distance = 0;
    
    // Loop through all 32 possible bit positions for 32-bit integers
    for (int i = 0; i < 32; i++) {
        int count_ones = 0;
        
        // Count how many numbers have the i-th bit set to 1
        for (int j = 0; j < numsSize; j++) {
            if ((nums[j] >> i) & 1) {
                count_ones++;
            }
        }
        
        // Numbers with the i-th bit set to 0
        int count_zeros = numsSize - count_ones;
        
        // Each combination of a '1' and a '0' adds 1 to the total distance
        total_distance += count_ones * count_zeros;
    }
    
    return total_distance;
}
